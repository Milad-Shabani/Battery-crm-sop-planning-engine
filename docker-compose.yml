# `docker compose up planning` generates the synthetic CRM + operations
# dataset and runs the full forecast -> capacity plan -> MRP -> CRM
# analytics -> Excel pipeline in one shot — no external services required.
services:
  planning:
    build: .
    volumes:
      - ./data:/app/data
